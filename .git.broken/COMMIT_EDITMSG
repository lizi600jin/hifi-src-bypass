v1.8.2：深度校验判定升级为三态（比特完美 / 无损上采样 / 有损降档）

- 之前判定只分「降档/其余」，请求率低于输出率时只显示『输出通道在跑』，
  用户无法区分无损上采样与比特完美，也不明白请求率为何与上次不同
- 新判定明确说明：请求率是【当前曲目】的实际采样率，HiRes 档位下 48k/96k/192k 都存在，
  换歌即变；请求率 < 输出率时给出把混音率对齐到请求率的操作提示
- 一加13 实测：96k HiRes 曲目走 direct_pcm_out @96000，判定正确标注比特完美
